// Place any operations that needs to be ran once sever start inside function register()
// NextJS doc: We recommend importing files with side effects using import from within your register function instead
// example:
// export async function register() {
//     await import('package-with-side-effect')
//   }
export async function register() {
  // import any modules here, not outside this function
}
